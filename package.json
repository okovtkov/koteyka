{
    "name": "koteyka",
    "private": true,
    "scripts": {
        "build": "weboops --mode build --publicPath /koteyka/ -o ./docs/",
        "dev": "weboops --mode development -o ./docs/"
    },
    "dependencies": {
        "@weboops/core": "^0.1.5"
    }
}
